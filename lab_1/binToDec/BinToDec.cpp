#include <iostream>
using namespace std;

int bin2Dec(const string& binaryString) {
	int ind = binaryString.length();

	char bin_arr[ind + 1];
	
	strcpy(bin_arr, binaryString.c_str());

	for (int i = 0; i < ind; i++)


}
